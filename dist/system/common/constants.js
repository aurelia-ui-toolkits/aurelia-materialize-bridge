'use strict';

System.register([], function (_export, _context) {
  "use strict";

  var constants;
  return {
    setters: [],
    execute: function () {
      _export('constants', constants = {
        eventPrefix: 'md-on-',
        bindablePrefix: 'md-'
      });

      _export('constants', constants);
    }
  };
});